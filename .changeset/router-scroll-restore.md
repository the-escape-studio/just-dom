---
"@just-dom/router": minor
---

Add scroll restoration (default `scroll: "restore"`): scroll to top on push/replace, restore on back/forward. Opt out with `scroll: false`. Use `preventScrollReset` on `routerLink` and `navigate`.
